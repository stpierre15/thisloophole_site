import { rankPriceGaps } from './price-gap-engine.mjs';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',minimumFractionDigits:0,maximumFractionDigits:2}).format(n);
const date=v=>new Date(v).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric',timeZone:'UTC'});
export function renderPriceBoard(entries){
 return rankPriceGaps(entries).map(e=>`<details class="gap-item" id="${esc(e.id)}"><summary class="gap-summary"><span class="rank">${String(e.rank).padStart(2,'0')}</span><span class="product-info"><span class="product-name">${esc(e.name)}</span><span class="product-variant">${esc(e.brand)} · ${esc(e.variant)} · See receipts +</span></span><span class="high-offer"><span class="mobile-label">HIGHEST</span><span class="offer-price">${money(e.highest.price)}</span><span class="offer-store">${esc(e.highest.store)}</span></span><span class="low-offer"><span class="mobile-label">LOWEST</span><span class="offer-price">${money(e.lowest.price)}</span><span class="offer-store">${esc(e.lowest.store)}</span></span><span class="premium">+${Math.round(e.premium)}%<small>${money(e.savings)} apart</small></span></summary><div class="gap-evidence"><h3>MODEL ${esc(e.model)} / UPC ${esc(e.barcode)}</h3><p>${esc(e.note)}</p><ul class="store-list">${e.offers.map(o=>`<li><div><a href="${esc(o.url)}" target="_blank" rel="noopener noreferrer" data-outbound="${esc(e.id)}">${esc(o.store)} ↗</a><small>${esc(o.deliveryNote)}</small></div><div><strong>${money(o.price)}</strong><small>Observed ${date(o.observedAt)} · <a href="${esc(o.evidenceUrl)}" target="_blank" rel="noopener noreferrer">Price record ↗</a></small></div></li>`).join('')}</ul><div class="entry-actions"><a class="text-link" href="${esc(e.lowest.url)}" target="_blank" rel="noopener noreferrer" data-outbound="${esc(e.id)}">Check the lower price ↗</a><button type="button" data-share="${esc(e.id)}">Share this gap ↗</button></div><p class="share-status" role="status"></p></div></details>`).join('');
}
export function renderMonthlyPick(entries){
 const e=rankPriceGaps(entries)[0];if(!e)return'';
 return `<div><p class="eyebrow">${esc(e.name.toUpperCase())} / ${esc(e.variant.toUpperCase())}</p><h3>The same pair.<br>${money(e.savings)} apart.</h3><p>Matching model number. Matching UPC. A different store asks ${Math.round(e.percentLess)}% less for the same listed product.</p><a class="text-link" href="/samething/#${esc(e.id)}">See the receipts ↗</a></div><div><div class="pick-prices"><s>${money(e.highest.price)} · ${esc(e.highest.store)}</s><span>${money(e.lowest.price)} · ${esc(e.lowest.store)}</span></div><p class="pick-stat">YOU KEEP ${money(e.savings)} BEFORE SHIPPING & TAX.</p><p class="fine-print">Prices observed September 16, 2026. Stock, delivery and returns may differ. Recheck both listings before ordering.</p></div>`;
}

