name = "thisloophole-proxy"
main = "src/index.js"
compatibility_date = "2024-11-01"

# After first deploy, set the secret (do not commit real keys):
#   cd worker && npx wrangler secret put ANTHROPIC_API_KEY

[vars]
# ANTHROPIC_API_KEY must be set as a secret in the Cloudflare dashboard or via wrangler secret put
