LATEST RELEASE — September 17, 2026
Plain document frame, no new product functionality.
Production: https://thisloophole.com
Netlify deployment: 6aacae35dff4b8d130942229
Routes: /, /experiments/, /findings/, /about/, preserved /samething/.
See README.md and QA.md for current scope and validation.
The older deployment instructions below describe previous releases; the purchase checker remains paused.

Loophole MVP — deploy to the EXISTING Netlify site
================================================

Repository: https://github.com/stpierre15/thisloophole_site
Local checkout: /Users/cecilstpierre/Desktop/loophole/loophole_v4
Production: https://thisloophole.com

Published 2026-09-11 to the existing Netlify project thisloophole. Production: https://thisloophole.com
Production deploy: 6aa39898ef7ff82f4cdae36f
Tested draft: 6aa3979579462c605dfc95af (purchase, useful vote and wait outcome all saved).
The pre-MVP GitHub main resolved to 37cc4bedd845b54869f16c0647be271dadf1c45b.
GitHub rejected the saved credential during a dry-run push; no remote changes were made.
Netlify CLI authorization now works and .netlify/state.json links this checkout to the existing project. The earlier GitHub credential failure is separate: source must still be pushed before relying on automatic Git deploys. The production deployment above used the local checkout via Netlify CLI.

1. From this existing checkout:

   cd /Users/cecilstpierre/Desktop/loophole/loophole_v4
   npm ci
   npm test
   npm run build
   npx netlify-cli build --offline

2. This checkout is already linked. For another machine, authenticate and link the existing project:

   npx netlify-cli login
   npx netlify-cli link

   Choose the existing Netlify project whose domain is thisloophole.com.
   Do NOT create a project. Inspect Project configuration > Domain management
   and confirm the domain before publishing. No DNS changes are needed.

3. Confirm deployment settings (already specified in netlify.toml):

   Base directory: repository root (the folder containing this file)
   Build command: npm run build
   Publish directory: dist
   Functions directory: netlify/functions
   Node version: 22

   Core purchase checks do not require API keys. Netlify Blobs is configured
   by the Netlify Functions runtime. Production data lives in
   loophole-v1-production; preview data uses a separate deploy namespace.
   The retired chatbot files are in legacy/ and incur no model API calls.

4. Optional: configure LOOPHOLE_ADMIN_TOKEN in the Netlify Functions scope
   for founder metrics. Use a fresh long random secret. Never put it in
   frontend code or Git. No admin token is needed for consumer checks.

5. Make an unpublished deployment first:

   npx netlify-cli deploy --build

   Open the returned draft URL. Submit a purchase, then useful feedback and
   a bought/wait outcome. Verify data exists in Netlify > Blobs and the
   functions log has no runtime errors. Draft deployment does not change
   thisloophole.com; its records do not mix with production records.

6. Publish to that SAME linked site:

   npx netlify-cli deploy --build --prod

   Verify https://thisloophole.com shows 'Before you buy it, Loophole it.'
   Check the purchase form and feedback again on the domain. Production
   function configuration and actual Blobs authorization require this
   hosted smoke test; an offline build cannot prove them.

7. Preserve the implementation in the existing GitHub repository. Review
   git diff and git status before committing because this checkout already
   contained local edits. The old edited chatbot source is retained in
   legacy/. Once the final source is committed and pushed, confirm Netlify's
   Git integration points at this repository and main. Future builds must
   use npm run build and dist, not publish the repository root.

   If a CLI deploy succeeds before Git is updated, an older Git deploy can
   overwrite it. Update the repository before the next automated deploy.

Rollback: in Netlify > Deploys, select the prior successful production
version and use Publish deploy. Site-wide production purchase and outcome
records survive rollback. Do not delete the Blobs store or change DNS.

Do not use drag-and-drop of dist alone: the site needs bundled Functions.
